# Fase 0A - servico `api`
# Versoes fixadas por reprodutibilidade (regra 13 do CLAUDE.md).
# Verificadas por instalacao real em 2026-09-01, Python 3.12.10.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0

# Incremento 5 - cerebro lento. Verificadas por instalacao real em 2026-09-02.
# `langgraph-sdk` exige websockets<17 e o uvicorn[standard] aceita >=13, entao
# instalar isto rebaixa websockets de 17.1 para 16.1.1. Nao ha conflito (o
# `pip check` passa) e nao usamos websocket em rota nenhuma - fica registrado
# porque um rebaixamento silencioso de dependencia transitiva e o tipo de
# coisa que so aparece no deploy.
langgraph==1.2.11
anthropic==1.3.0
openai==3.7.0

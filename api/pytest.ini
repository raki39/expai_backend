[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    rede: exige chamada real a um provedor de LLM. Pulado sem chave, e o
        relatorio do incremento declara que nao rodou - um teste pulado em
        silencio vira criterio dado como cumprido sem ter sido.
